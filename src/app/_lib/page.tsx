import React from 'react';

// Private folders can be created by prefixing a folder with an underscore: _folderName
const PrivateFolder = () => {
    return (
        <div>
            This folder is private and can't see anything in broeser
        </div>
    );
};

export default PrivateFolder;